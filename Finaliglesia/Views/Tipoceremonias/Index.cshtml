@model IEnumerable<Finaliglesia.Models.Tipoceremonia>

@{
    ViewBag.Title = "Index";
}

<h2>Inicio</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Detalle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Costo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Detalle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Costo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.TipoceremoniaID }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.TipoceremoniaID }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.TipoceremoniaID })
        </td>
    </tr>
}

</table>
