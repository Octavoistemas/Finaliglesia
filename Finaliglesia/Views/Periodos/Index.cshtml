@model IEnumerable<Finaliglesia.Models.Periodo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Detalle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Detalle)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.PeriodoID }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.PeriodoID }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.PeriodoID })
        </td>
    </tr>
}

</table>
