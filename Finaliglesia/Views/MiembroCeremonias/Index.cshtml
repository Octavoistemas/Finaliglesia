@model IEnumerable<Finaliglesia.Models.MiembroCeremonia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rol)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MiembrosId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CeremoniasId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rol)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MiembrosId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CeremoniasId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MiembroCeremoniaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MiembroCeremoniaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MiembroCeremoniaID })
        </td>
    </tr>
}

</table>
