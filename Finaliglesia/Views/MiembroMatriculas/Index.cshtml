@model IEnumerable<Finaliglesia.Models.MiembroMatricula>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MiembrosId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MatriculasId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MiembrosId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MatriculasId)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.MiembroMatriculaID }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.MiembroMatriculaID }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.MiembroMatriculaID })
        </td>
    </tr>
}

</table>
