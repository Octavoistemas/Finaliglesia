@model IEnumerable<Finaliglesia.Models.MiembroMatricula>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MiembrosId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MatriculasId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MiembrosId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MatriculasId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MiembroMatriculaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MiembroMatriculaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MiembroMatriculaID })
        </td>
    </tr>
}

</table>
